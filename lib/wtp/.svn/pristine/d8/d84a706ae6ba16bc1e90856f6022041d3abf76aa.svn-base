/*
 * rfm22callbacks.cpp
 *
 *  Created on: 4 lis 2013
 *      Author: Maciek
 */

#include "rfm22callbacks.h"
#include "rfm22bands.h"
#include "stm32l0xx_hal.h"

/**
 * for STANDARD_CHANNEL implement rfm22callbacks_delay_ms(...), other functions can be stubs
 * for CUSTOM_CHANNEL implement all functions, setters should save data permanently - e.g. in eeprom
 */

/**
 * If custom region RFM22BANDS_REGION_x_CUSTOM_CHANNEL is used, uncomment following variables,
 * modify them to be stored in a permanent memory (e.g. eeprom) and implement their setters/getters
 */
//static const uint8_t rfm22callbacks_channel = RFM22BANDS_MENU_SELECTION_DEFAULT_CHANNEL;
//static const uint8_t rfm22callbacks_power = RFM22BANDS_MENU_SELECTION_DEFAULT_POWER;

/*channel*/
/*--------------------------------------------------------------------*/
void rfm22callbacks_set_channel(uint8_t channel)
{

}

/*--------------------------------------------------------------------*/
uint8_t rfm22callbacks_get_channel()
{
    return 0;
}

/*--------------------------------------------------------------------*/
void rfm22callbacks_set_power(uint8_t power)
{

}

/*--------------------------------------------------------------------*/
uint8_t rfm22callbacks_get_power()
{
    return 0;
}

/*--------------------------------------------------------------------*/
void rfm22callbacks_delay_ms(uint16_t ms)
{
    HAL_Delay(ms);
}
